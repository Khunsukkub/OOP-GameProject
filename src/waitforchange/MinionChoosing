ช่วย validate จำนวนมินเนี่ยนที่เลือก (ใช้เป็น utility ในกรณีที่จำเป็น)
package model;

public class MinionChoosing {
    public static int chooseMinionNumber(int number) {
        if (number < 1 || number > 5) {
            return 1;
        }
        return number;
    }
}
